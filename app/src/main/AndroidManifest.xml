<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.joshua.r0th.dataaset">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".LoginActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RegisterActivity"
            android:label="Registrasi"
            android:theme="@style/AppTheme"/>
        <activity android:name=".splashafterlogin"
            android:label="splashafterlogin"
            android:theme="@style/AppTheme"/>
        <activity android:name=".MainActivityAdmin"
            android:label="MainActivityAdmin"
            android:theme="@style/AppTheme"/>
        <activity android:name=".MainActivity"
            android:label="MainActivity"
            android:theme="@style/AppTheme"/>
        <activity android:name="com.joshua.r0th.dataaset.ui.tambah_aset.tambah_asetAdmin"
            android:label="MainActivity"
            android:theme="@style/AppTheme"/>
        <activity android:name="com.joshua.r0th.dataaset.ui.lihat_aset.lihat_asetAdmin"
            android:label="MainActivity"
            android:theme="@style/AppTheme"/>
    </application>

</manifest>